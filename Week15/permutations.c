#include <stdio.h>
#include <stdlib.h>

int swap(int A[], int n)
{
    for (int i = 0; i < n; i++)
    {
        /* code */
    }
}

int main()
{
    int n;
    scanf("%d", &n);
    int A[n];
    for (int i = 0; i < n; i++)
    {
        A[i] = i + 1;
    }
}